# ShelfSpace API

Постоянная книжная полка на FastAPI, SQLModel и SQLite.

```bash
uv sync --dev
uv run fastapi dev app/main.py
```

После первого запуска должен появиться локальный файл БД. Он исключён из Git. Проверка: `uv run pytest`.
