[project]
name = "shelfspace-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi[standard]>=0.139,<1", "sqlmodel>=0.0.39,<1"]

[dependency-groups]
dev = ["httpx>=0.28,<1", "pytest>=8,<10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
